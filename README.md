# Flagship Predictability

## Recommended execution order

- notebook 00 - 11 in order

## Environment assumptions

- WeatherBench2-style local Zarr stores on disk
- Python packages: `numpy`, `pandas`, `xarray`, `matplotlib`, `scikit-learn`

## Output layout

Outputs default to `notebooks/outputs` unless `FLAGSHIP_OUTPUT_ROOT` is set.



